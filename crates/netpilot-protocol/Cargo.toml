[package]
name = "netpilot-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
async-trait = "0.1"
netpilot-config = { path = "../netpilot-config" }
thiserror.workspace = true
tokio.workspace = true
