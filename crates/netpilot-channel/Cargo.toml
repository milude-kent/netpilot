[package]
name = "netpilot-channel"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
netpilot-config = { path = "../netpilot-config" }
netpilot-filter = { path = "../netpilot-filter" }
netpilot-rib = { path = "../netpilot-rib" }
thiserror.workspace = true
